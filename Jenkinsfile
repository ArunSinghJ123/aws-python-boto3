pipeline {
    agent any
    stages {
        stage('stage one') {
            steps {
                echo "testing build"
            }
        }
        stage('stage two') {
            steps {
                echo "testing this declarative build sample"
            }
        }
    }
}
